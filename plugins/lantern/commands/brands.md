---
description: List the Lantern brands you can access and pick one for this session.
---

Call `findings.get_visibility_brands` (no arguments). Present the returned brands by name (with a short note that the UUID is used internally). Ask the user which brand they want to work with, and remember that `accountBrandId` for subsequent Lantern requests in this session.
